<?php 
/** 
 * @file 
 * Primary pre/preprocess functions and alterations.
 */
function rbv_kala_default_preprocess_node(&$vars) {
  if ($vars['node']->type == 'org' && $vars['view_mode'] == 'default_full') {
    $vars['theme_hook_suggestions'][] = 'node__org__default_full';
  }
}
